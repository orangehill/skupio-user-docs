{
  "name": "skupio-user-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Content + CI for docs.skupio.co. Renderer lives in orangehill/skupio-docs-site.",
  "type": "module",
  "scripts": {
    "leak-check": "node scripts/leak-check.mjs",
    "validate:frontmatter": "node scripts/validate-frontmatter.mjs",
    "check": "npm run leak-check && npm run validate:frontmatter"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "globby": "^14.0.2",
    "gray-matter": "^4.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
